---
const values = [
  {
    icon: '🎯',
    title: 'Excelencia',
    description: 'Resultados excepcionales respaldados por formación especializada y actualización constante.'
  },
  {
    icon: '💝',
    title: 'Personalización',
    description: 'Cada tratamiento se adapta a tus necesidades específicas y objetivos personales.'
  },
  {
    icon: '🔬',
    title: 'Innovación',
    description: 'Tecnología de vanguardia y productos certificados para garantizar tu seguridad.'
  },
  {
    icon: '🤝',
    title: 'Confianza',
    description: 'Transparencia en cada proceso, acompañándote desde la consulta hasta tu recuperación.'
  }
];
---

<section class="about" id="nosotros">
  <div class="container">
    <div class="about-grid">
      <div class="about-visual reveal-left">
        <div class="about-image-container">
          <div class="about-image">
            <svg viewBox="0 0 500 600" fill="none" xmlns="http://www.w3.org/2000/svg">
              <rect width="500" height="600" rx="16" fill="url(#aboutGradient)"/>
              <circle cx="250" cy="200" r="100" fill="#F5EBEB"/>
              <rect x="150" y="320" width="200" height="220" rx="8" fill="#E8D5D5"/>
              <rect x="180" y="350" width="140" height="8" rx="4" fill="#D4B8B8"/>
              <rect x="180" y="380" width="100" height="8" rx="4" fill="#D4B8B8"/>
              <rect x="180" y="410" width="120" height="8" rx="4" fill="#D4B8B8"/>
              <circle cx="170" cy="180" r="10" fill="#2D3436" opacity="0.2"/>
              <circle cx="230" cy="180" r="10" fill="#2D3436" opacity="0.2"/>
              <path d="M185 220 Q207 240 230 220" stroke="#D4B8B8" stroke-width="4" fill="none" stroke-linecap="round"/>
              <defs>
                <linearGradient id="aboutGradient" x1="0" y1="0" x2="500" y2="600">
                  <stop offset="0%" stop-color="#F7F3EF"/>
                  <stop offset="100%" stop-color="#EDE8E3"/>
                </linearGradient>
              </defs>
            </svg>
          </div>
          
          <div class="about-experience">
            <span class="experience-number">+10</span>
            <span class="experience-text">Años de<br/>experiencia</span>
          </div>
        </div>
      </div>
      
      <div class="about-content reveal-right">
        <span class="section-label">Sobre Nosotros</span>
        <h2 class="about-title">
          Bienestar físico y<br/>
          <span class="text-accent">belleza integral</span>
        </h2>
        
        <p class="about-lead">
          No solo nos enfocamos en embellecimiento, sino también en promover el 
          bienestar físico integral de cada paciente.
        </p>
        
        <p class="about-text">
          Nuestro equipo de profesionales está comprometido en facilitar el proceso 
          y la recuperación de las intervenciones, minimizando complicaciones asociadas 
          como el edema en el período pre-trans-post operatorio y la cicatrización 
          postquirúrgica.
        </p>
        
        <p class="about-text">
          Nuestro objetivo es brindar un servicio completo que combine resultados 
          estéticos excepcionales con una atención personalizada.
        </p>
        
        <div class="about-values">
          {values.map((value) => (
            <div class="value-item">
              <span class="value-icon">{value.icon}</span>
              <div class="value-content">
                <h4 class="value-title">{value.title}</h4>
                <p class="value-description">{value.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: var(--space-5xl) 0;
    background: var(--bg-primary);
  }
  
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }
  
  .about-visual {
    position: relative;
  }
  
  .about-image-container {
    position: relative;
  }
  
  .about-image {
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }
  
  .about-image svg {
    width: 100%;
    height: auto;
    display: block;
  }
  
  .about-experience {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background: var(--color-primary);
    color: var(--color-white);
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    text-align: center;
    box-shadow: var(--shadow-lg);
  }
  
  .experience-number {
    display: block;
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 600;
    line-height: 1;
  }
  
  .experience-text {
    display: block;
    font-size: var(--text-xs);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    opacity: 0.8;
    margin-top: var(--space-xs);
  }
  
  .about-content {
    max-width: 560px;
  }
  
  .about-title {
    font-size: var(--text-4xl);
    margin-bottom: var(--space-xl);
  }
  
  .about-lead {
    font-size: var(--text-lg);
    color: var(--color-primary);
    font-weight: 500;
    margin-bottom: var(--space-lg);
    line-height: 1.7;
  }
  
  .about-text {
    font-size: var(--text-base);
    color: var(--color-secondary);
    margin-bottom: var(--space-md);
    line-height: 1.8;
  }
  
  .about-values {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
    margin-top: var(--space-2xl);
    padding-top: var(--space-2xl);
    border-top: 1px solid var(--bg-tertiary);
  }
  
  .value-item {
    display: flex;
    gap: var(--space-md);
  }
  
  .value-icon {
    font-size: var(--text-xl);
    flex-shrink: 0;
  }
  
  .value-content {
    flex: 1;
  }
  
  .value-title {
    font-family: var(--font-display);
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
  }
  
  .value-description {
    font-size: var(--text-sm);
    color: var(--color-secondary);
    line-height: 1.6;
    margin-bottom: 0;
  }
  
  @media (max-width: 992px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    
    .about-visual {
      order: -1;
      max-width: 400px;
      margin: 0 auto;
    }
    
    .about-content {
      max-width: 100%;
      text-align: center;
    }
    
    .about-values {
      text-align: left;
    }
    
    .about-experience {
      right: 0;
      bottom: -10px;
    }
  }
  
  @media (max-width: 576px) {
    .about-values {
      grid-template-columns: 1fr;
    }
    
    .about-experience {
      padding: var(--space-md);
    }
    
    .experience-number {
      font-size: var(--text-2xl);
    }
  }
</style>