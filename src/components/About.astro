---
// About Component - Fisiodermy
const listItems = [
  {
    title: 'Profesionales Certificados',
    description: 'Equipo especializado en fisioterapia dermatofuncional'
  },
  {
    title: 'Tecnología de Vanguardia',
    description: 'Equipamiento de última generación para mejores resultados'
  },
  {
    title: 'Atención Integral',
    description: 'Acompañamiento completo en cada etapa de tu tratamiento'
  }
];
---

<section class="about" id="nosotros">
  <div class="container">
    <div class="about-grid">
      <div class="about-visual animate-on-scroll from-left">
        <div class="about-image-main">
          <span>Imagen del equipo</span>
        </div>
        <div class="about-image-accent"></div>
        <div class="about-frame"></div>
        
        <div class="about-badge">
          <div class="about-badge-icon">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              <path d="m9 12 2 2 4-4"/>
            </svg>
          </div>
          <p class="about-badge-text">Calidad Garantizada</p>
        </div>
      </div>
      
      <div class="about-content animate-on-scroll from-right delay-2">
        <p class="section-label">Sobre Nosotros</p>
        <h2 class="about-title">
          Compromiso con tu <em>bienestar</em>
        </h2>
        <p class="about-text">
          Nuestro equipo de profesionales está comprometido en facilitar el proceso y la 
          recuperación de las intervenciones, minimizando complicaciones asociadas como el 
          edema en el período pre-trans-post operatorio y la cicatrización postquirúrgica.
        </p>
        <p class="about-text">
          Nuestro objetivo es brindar un servicio completo que combine resultados estéticos 
          excepcionales con una atención personalizada.
        </p>
        
        <div class="about-list">
          {listItems.map((item, index) => (
            <div class={`about-list-item animate-on-scroll delay-${index + 3}`}>
              <div class="about-list-icon">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="m9 12 2 2 4-4"/>
                  <circle cx="12" cy="12" r="10"/>
                </svg>
              </div>
              <div class="about-list-content">
                <h4>{item.title}</h4>
                <p>{item.description}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: var(--section-padding) 0;
    background: var(--white);
    overflow: hidden;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 100px;
    align-items: center;
  }

  /* Visual */
  .about-visual {
    position: relative;
  }

  .about-image-main {
    width: 100%;
    height: 650px;
    background: linear-gradient(135deg, var(--black-soft) 0%, var(--black-medium) 100%);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .about-image-main span {
    font-family: var(--font-display);
    font-size: 14px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.15);
  }

  .about-image-accent {
    position: absolute;
    bottom: -50px;
    right: -50px;
    width: 280px;
    height: 320px;
    background: var(--gold);
    opacity: 0.08;
  }

  .about-frame {
    position: absolute;
    top: -30px;
    left: -30px;
    width: 200px;
    height: 200px;
    border: 1px solid var(--gold);
    opacity: 0.4;
    animation: borderGlow 4s ease-in-out infinite;
  }

  .about-badge {
    position: absolute;
    bottom: 60px;
    left: -70px;
    background: var(--black);
    padding: 40px 45px;
    text-align: center;
    box-shadow: var(--shadow-xl);
    animation: float 5s ease-in-out infinite;
  }

  .about-badge-icon {
    width: 45px;
    height: 45px;
    margin: 0 auto 15px;
  }

  .about-badge-icon svg {
    width: 100%;
    height: 100%;
    color: var(--gold);
  }

  .about-badge-text {
    font-family: var(--font-display);
    font-size: 15px;
    font-style: italic;
    color: var(--white);
    letter-spacing: 1px;
  }

  /* Content */
  .about-content {
    padding-left: 30px;
  }

  .about-title {
    color: var(--black);
    margin-bottom: 30px;
    line-height: 1.2;
  }

  .about-title em {
    font-style: italic;
    color: var(--gold);
  }

  .about-text {
    font-size: 16px;
    color: var(--gray);
    line-height: 2;
    margin-bottom: 20px;
  }

  .about-list {
    margin-top: 45px;
    padding-top: 45px;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }

  .about-list-item {
    display: flex;
    align-items: flex-start;
    gap: 20px;
    margin-bottom: 30px;
    padding: 15px;
    transition: all 0.3s var(--transition-smooth);
  }

  .about-list-item:last-child {
    margin-bottom: 0;
  }

  .about-list-item:hover {
    background: var(--white-soft);
    transform: translateX(10px);
  }

  .about-list-icon {
    width: 55px;
    height: 55px;
    background: rgba(201, 162, 39, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: all 0.3s;
  }

  .about-list-item:hover .about-list-icon {
    background: var(--gold);
  }

  .about-list-icon svg {
    width: 24px;
    height: 24px;
    color: var(--gold);
    transition: color 0.3s;
  }

  .about-list-item:hover .about-list-icon svg {
    color: var(--black);
  }

  .about-list-content h4 {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 600;
    color: var(--black);
    margin-bottom: 5px;
  }

  .about-list-content p {
    font-size: 13px;
    font-weight: 300;
    color: var(--gray);
  }

  @media (max-width: 992px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 60px;
    }

    .about-content {
      padding-left: 0;
    }

    .about-badge {
      position: relative;
      left: 0;
      bottom: 0;
      margin-top: 30px;
      display: inline-block;
    }

    .about-image-main {
      height: 500px;
    }
  }
</style>
