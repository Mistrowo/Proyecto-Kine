---
// Hero Component - Fisiodermy
---

<section class="hero" id="inicio">
  <!-- Background Effects -->
  <div class="hero-bg"></div>
  <div class="hero-pattern"></div>
  <div class="hero-gradient-orb hero-gradient-orb-1"></div>
  <div class="hero-gradient-orb hero-gradient-orb-2"></div>
  
  <!-- Decorative Lines -->
  <div class="hero-lines">
    <div class="hero-line hero-line-1"></div>
    <div class="hero-line hero-line-2"></div>
    <div class="hero-line hero-line-3"></div>
    <div class="hero-line hero-line-4"></div>
  </div>

  <!-- Floating Elements -->
  <div class="hero-float hero-float-1"></div>
  <div class="hero-float hero-float-2"></div>
  
  <div class="container">
    <div class="hero-content">
      <span class="hero-label">
        <span class="hero-label-line"></span>
        Estética Avanzada
        <span class="hero-label-line"></span>
      </span>
      
      <h1 class="hero-title">
        Luce tu <em class="gold-shimmer">mejor versión</em>
      </h1>
      
      <p class="hero-text">
        Especialistas en fisioterapia dermatofuncional, tratamientos pre y post operatorio, 
        recuperación quirúrgica y bienestar físico. Resultados excepcionales con atención personalizada.
      </p>
      
      <div class="hero-buttons">
        <a href="#contacto" class="btn btn-gold">
          <span>Agendar Consulta</span>
        </a>
        <a href="#servicios" class="btn btn-outline">
          <span>Explorar Servicios</span>
        </a>
      </div>
    </div>
  </div>
  
  <!-- Visual Frame -->
  <div class="hero-visual">
    <div class="hero-visual-frame">
      <div class="hero-visual-frame-inner"></div>
    </div>
    <div class="hero-visual-content">
      <span>Imagen Hero</span>
    </div>
    <div class="hero-visual-badge">
      <span class="hero-visual-badge-icon">✦</span>
      <span class="hero-visual-badge-text">Premium</span>
    </div>
  </div>
  
  <!-- Scroll Indicator -->
  <div class="hero-scroll">
    <span>Scroll</span>
    <div class="hero-scroll-line"></div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    background: var(--black);
    position: relative;
    overflow: hidden;
  }

  /* Background Effects */
  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--black) 0%, var(--black-rich) 50%, var(--black-soft) 100%);
  }

  .hero-pattern {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.02;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23c9a227' fill-opacity='1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  }

  .hero-gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.15;
  }

  .hero-gradient-orb-1 {
    top: 10%;
    left: 10%;
    width: 500px;
    height: 500px;
    background: var(--gold);
    animation: float 8s ease-in-out infinite;
  }

  .hero-gradient-orb-2 {
    bottom: 10%;
    right: 10%;
    width: 400px;
    height: 400px;
    background: var(--gold-dark);
    animation: float 10s ease-in-out infinite reverse;
  }

  /* Decorative Lines */
  .hero-lines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .hero-line {
    position: absolute;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    opacity: 0.1;
  }

  .hero-line-1 {
    top: 20%;
    left: 0;
    width: 35%;
    height: 1px;
    animation: fadeInLeft 1s ease 0.5s both;
  }

  .hero-line-2 {
    bottom: 25%;
    right: 0;
    width: 30%;
    height: 1px;
    animation: fadeInRight 1s ease 0.7s both;
  }

  .hero-line-3 {
    top: 15%;
    right: 20%;
    width: 1px;
    height: 15%;
    background: linear-gradient(180deg, transparent, var(--gold), transparent);
    animation: fadeInDown 1s ease 0.9s both;
  }

  .hero-line-4 {
    bottom: 20%;
    left: 15%;
    width: 1px;
    height: 10%;
    background: linear-gradient(180deg, transparent, var(--gold), transparent);
    animation: fadeInUp 1s ease 1.1s both;
  }

  /* Floating Elements */
  .hero-float {
    position: absolute;
    border: 1px solid rgba(201, 162, 39, 0.2);
    animation: float 6s ease-in-out infinite;
  }

  .hero-float-1 {
    top: 30%;
    right: 25%;
    width: 80px;
    height: 80px;
    transform: rotate(45deg);
    animation-delay: 0s;
  }

  .hero-float-2 {
    bottom: 35%;
    left: 20%;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    animation-delay: 2s;
  }

  /* Content */
  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 750px;
    padding: 160px 0 120px;
  }

  .hero-label {
    display: inline-flex;
    align-items: center;
    gap: 20px;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 35px;
    animation: fadeInUp 1s ease 0.3s both;
  }

  .hero-label-line {
    width: 50px;
    height: 1px;
    background: var(--gold);
  }

  .hero-title {
    font-size: clamp(48px, 7vw, 85px);
    font-weight: 300;
    color: var(--white);
    margin-bottom: 30px;
    letter-spacing: -2px;
    line-height: 1.1;
    animation: fadeInUp 1s ease 0.5s both;
  }

  .hero-title em {
    font-style: italic;
    font-weight: 400;
  }

  .hero-text {
    font-size: 17px;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 50px;
    max-width: 550px;
    line-height: 1.9;
    animation: fadeInUp 1s ease 0.7s both;
  }

  .hero-buttons {
    display: flex;
    gap: 25px;
    animation: fadeInUp 1s ease 0.9s both;
  }

  /* Visual Frame */
  .hero-visual {
    position: absolute;
    right: 5%;
    top: 50%;
    transform: translateY(-50%);
    width: 420px;
    height: 520px;
    animation: fadeIn 1.5s ease 1s both;
  }

  .hero-visual-frame {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: 1px solid rgba(201, 162, 39, 0.3);
    animation: borderGlow 3s ease-in-out infinite;
  }

  .hero-visual-frame-inner {
    position: absolute;
    top: 25px;
    left: 25px;
    right: -25px;
    bottom: -25px;
    border: 1px solid rgba(201, 162, 39, 0.15);
  }

  .hero-visual-content {
    position: absolute;
    top: 40px;
    left: 40px;
    right: 40px;
    bottom: 40px;
    background: linear-gradient(135deg, var(--black-soft) 0%, var(--black-medium) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-visual-content span {
    font-family: var(--font-display);
    font-size: 14px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.15);
  }

  .hero-visual-badge {
    position: absolute;
    bottom: -20px;
    left: -20px;
    background: var(--gold);
    padding: 20px 25px;
    display: flex;
    align-items: center;
    gap: 10px;
    animation: scaleIn 0.6s ease 1.5s both;
  }

  .hero-visual-badge-icon {
    font-size: 18px;
    color: var(--black);
  }

  .hero-visual-badge-text {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--black);
  }

  /* Scroll Indicator */
  .hero-scroll {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    animation: fadeIn 1s ease 1.5s both;
  }

  .hero-scroll span {
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.4);
  }

  .hero-scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(180deg, var(--gold), transparent);
    animation: pulse 2s ease-in-out infinite;
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .hero-visual {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      padding: 140px 0 100px;
    }

    .hero-buttons {
      flex-direction: column;
    }

    .hero-label-line {
      width: 30px;
    }

    .hero-float,
    .hero-lines {
      display: none;
    }
  }
</style>
