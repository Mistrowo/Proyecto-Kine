---
// CTA Component - Fisiodermy
---

<section class="cta">
  <div class="cta-pattern"></div>
  
  <div class="container">
    <div class="cta-content animate-on-scroll">
      <h2 class="cta-title">
        ¿Lista para lucir tu <em>mejor versión</em>?
      </h2>
      <p class="cta-text">
        Agenda tu evaluación personalizada y comienza tu transformación
      </p>
      <a href="#contacto" class="btn btn-black">
        <span>Reservar Consulta</span>
      </a>
    </div>
  </div>
  
  <!-- Corner Decorations -->
  <div class="cta-corner cta-corner-tl"></div>
  <div class="cta-corner cta-corner-tr"></div>
  <div class="cta-corner cta-corner-bl"></div>
  <div class="cta-corner cta-corner-br"></div>
</section>

<style>
  .cta {
    padding: 130px 0;
    background: linear-gradient(135deg, var(--gold-dark) 0%, var(--gold) 50%, var(--gold-light) 100%);
    position: relative;
    overflow: hidden;
  }

  .cta-pattern {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23000000' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    opacity: 0.5;
  }

  .cta-content {
    position: relative;
    z-index: 2;
    text-align: center;
  }

  .cta-title {
    font-size: clamp(36px, 5vw, 56px);
    color: var(--black);
    margin-bottom: 20px;
  }

  .cta-title em {
    font-style: italic;
  }

  .cta-text {
    font-size: 18px;
    font-weight: 300;
    color: rgba(0, 0, 0, 0.7);
    margin-bottom: 45px;
  }

  /* Corner Decorations */
  .cta-corner {
    position: absolute;
    width: 80px;
    height: 80px;
    border: 2px solid rgba(0, 0, 0, 0.1);
  }

  .cta-corner-tl {
    top: 40px;
    left: 40px;
    border-right: none;
    border-bottom: none;
  }

  .cta-corner-tr {
    top: 40px;
    right: 40px;
    border-left: none;
    border-bottom: none;
  }

  .cta-corner-bl {
    bottom: 40px;
    left: 40px;
    border-right: none;
    border-top: none;
  }

  .cta-corner-br {
    bottom: 40px;
    right: 40px;
    border-left: none;
    border-top: none;
  }

  @media (max-width: 768px) {
    .cta-corner {
      width: 40px;
      height: 40px;
    }

    .cta-corner-tl,
    .cta-corner-tr {
      top: 20px;
    }

    .cta-corner-bl,
    .cta-corner-br {
      bottom: 20px;
    }

    .cta-corner-tl,
    .cta-corner-bl {
      left: 20px;
    }

    .cta-corner-tr,
    .cta-corner-br {
      right: 20px;
    }
  }
</style>
